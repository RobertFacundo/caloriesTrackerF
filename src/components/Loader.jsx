import { Spinner, LoaderContainer } from '../styled/components/LoaderStyled';

const Loader = () => {
    return (
        <LoaderContainer>
            <Spinner />
        </LoaderContainer>
    )
};

export default Loader;